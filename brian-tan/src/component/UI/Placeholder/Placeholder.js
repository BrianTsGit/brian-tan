import React from 'react';

import classes from './Placeholder.scss';

const placeholder = () => {
    return (
        <div className={classes.Placeholder}>
            <span>Nothing here yet.</span>
        </div>
    );
};

export default placeholder;